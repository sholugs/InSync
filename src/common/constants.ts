export enum Routes {
    AUTH = 'auth',
    USERS = 'users',
    USERS_PROFILES = 'users/profiles',
}

export enum Services {
    AUTH = 'AUTH_SERVICE',
    USERS = 'USERS_SERVICE',
    USERS_PROFILES = 'USERS_PROFILES_SERVICE',
}
